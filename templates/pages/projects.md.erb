[General info](/).

## Projects

<% PROJECTS_TYPES.each do |_projects_type, projects_type_title| %>
*   [<%= projects_type_title %>](#<%=
      projects_type_title.to_s.downcase.delete('()').tr(' ', '-')
    %>)
<% end %>

<%= render_partial :projects_list, projects: data['projects'], section_classes: %w[page projects] %>
