<% data.projects.each do |project| %>
*   ### <%
      if project.link
    %><%= external_link project.title, project.link %><%
      else
    %><%= project.title %><%
      end
    %>

    #### <%= project.begin_date %> – <%= project.end_date %>, <%= project.duration %>

    #### <%= project.position %>

    <% if project.team %>
    #### Team of <%= project.team %>
    {: .team }
    <% end %>

    <%= render project.description.split("\n").join("\n    ") %>

    Technologies:
    <% project.technologies.each do |technology| %>
    *   <%= technology %><% end %>
<% end %>{: .projects <%= '.' + additional_classes.join(' .') if additional_classes.any? %> }
